// import { db } from 'src/database';

// export const getPasswords = (_, res) => {
//   const query = 'SELECT * from passwords';

//   db.query(query, (err, data) => {
//     if (err) return res.json(err);

//     return res.status(200).json(data);
//   });
// };
